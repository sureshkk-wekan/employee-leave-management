# Copy to .env and adjust. Do not commit .env.
# API base URL (no trailing slash).
# - /api = use Vite proxy (requests go to same origin, proxy forwards to backend)
# - http://localhost:8000/api = call backend directly (no proxy; use if /api gives 401)
VITE_API_URL=/api
